# Changelog

## Unreleased

- 2025-10-22: Initial migration scaffold added.
  - Added UI primitives: `SimpleSuggester`, `PromptModal`, `YesNoModal`.
  - Added `ConfigService` with `readConfig` and `writeConfig`.
  - Added `CourseService` skeleton with `listCourses()`.
  - Added `Study: Open Tools` command to demo integration.
  - Added migration planning docs.
